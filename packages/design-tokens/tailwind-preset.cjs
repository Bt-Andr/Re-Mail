// Minimal valid CommonJS Tailwind v3 preset — stub pending real design tokens.
// Kept as plain .cjs (not .ts) so Tailwind's own config loader can require() it
// independent of any consuming app's "type": "module" setting.
module.exports = {
  theme: {
    extend: {},
  },
};
